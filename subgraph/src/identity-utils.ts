// src/identity-utils.ts
// Placeholder for identity utilities (e.g., mapping PoH attestations). Expand as needed.

export function normalizeAddress(addr: string): string {
  // The Graph already uses hex lowercased addresses in `toHexString()` for Bytes.
  return addr;
}
